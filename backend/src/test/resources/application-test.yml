global:
  location:
    image: res/test_images/

logging:
  file.name: ./log/backend-test.log

spring:
  datasource:
    url: "jdbc:h2:file:./database/testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=RUNSCRIPT FROM 'classpath:sql/modulo.sql'"
    driver-class-name: org.h2.Driver
    username: admin
    password: 12345
  jpa:
    hibernate:
      ddl-auto: none
